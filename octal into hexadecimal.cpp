#include<stdio.h>
#include<conio.h>
int  main() {
	int octal;
	printf("Enter the number in octal:");
	scanf("%o",&octal);
	printf("\nThe number in hexadecimal is=%x",octal);
	getch();
	return 0;
}

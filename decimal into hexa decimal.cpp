#include<stdio.h>
#include<conio.h>
int  main() {
	int decimal;
	printf("Enter the number in decimal:");
	scanf("%u",&decimal);
	printf("\nThe number in hexadecimal is=%x",decimal);
	getch();
	return 0;
}

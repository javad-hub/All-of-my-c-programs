#include<stdio.h>
#include<conio.h>
int  main() {
int x;
printf("Enter the number:");
scanf("%d",&x);
if(x%2==0)
printf("\nThe number is even");
else
printf("/tThe number is odd");
getch();
return 0;
}
